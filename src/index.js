import * as canvas from './canvas';
import {keysDown} from './keyMonitor';
import * as diagnostics from './diagnostics';

canvas.init(ctx => {

  // diagnostics.display(ctx, 'stuff to debug here');
});